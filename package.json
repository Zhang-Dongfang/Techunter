{
  "name": "techunter-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Techunter CLI and desktop applications",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev:desktop",
    "dev:api": "npm run dev --workspace @techunter/api",
    "dev:desktop": "npm run dev --workspace @techunter/desktop",
    "dev:cli": "tsx apps/cli/src/index.ts",
    "build": "npm run build --workspace @techunter/core && npm run build --workspace techunter && npm run build --workspace @techunter/api && npm run build --workspace @techunter/desktop",
    "typecheck": "npm run typecheck --workspace @techunter/core && npm run typecheck --workspace techunter && npm run typecheck --workspace @techunter/api && npm run typecheck --workspace @techunter/desktop",
    "test": "npm run test --workspace @techunter/api && npm run test --workspace @techunter/desktop",
    "start": "npm run start --workspace @techunter/api"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  },
  "overrides": {
    "esbuild": "^0.28.2"
  }
}
